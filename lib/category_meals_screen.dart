import 'package:flutter/material.dart';

class CateogyMealScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        actions: [],
        title: Text('Meal Screen'),
      ),
      body: Container(
        child: Text('Category Meal Screen'),
      ),
    );
  }
}
